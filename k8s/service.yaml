apiVersion: v1
kind: Service
metadata:
  name: mediahub-api-service
  namespace: mediahub
spec:
  type: ClusterIP
  selector:
    app: mediahub-api
  ports:
    - name: http
      port: 80
      targetPort: 4000
